# # How many except statements can a try-except block have? Name Some built-in exception classes:

# A try block can have multiple except statements to handle different exceptions.

# Some built-in exception classes: ValueError, TypeError, ZeroDivisionError, IndexError, KeyError, FileNotFoundError, NameError, ImportError, AttributeError, SyntaxError.